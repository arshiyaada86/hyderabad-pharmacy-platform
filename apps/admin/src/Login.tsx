import React,{useState} from 'react';
import { Alert, Box, Button, Stack, TextField, Typography } from '@mui/material';
import { LocalHospitalOutlined } from '@mui/icons-material';
import { api } from './api';
export default function Login({onLogin}:{onLogin:(user:any)=>void}){
 const [email,setEmail]=useState('owner@oldcity.local'),[password,setPassword]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');
 return <Box className="login"><Box className="login-art"><LocalHospitalOutlined sx={{fontSize:48,mb:5}}/><Typography variant="h3" fontWeight={750}>Care for your city.<br/>Manage it all here.</Typography><Typography sx={{mt:3,maxWidth:420,opacity:.75}}>One connected workspace for medicines, orders, doctors and the people you serve.</Typography><Box className="login-pattern"/></Box><Box className="login-form"><Typography className="eyebrow">OLD CITY PHARMACY</Typography><Typography variant="h4" mb={1}>Welcome back</Typography><Typography color="text.secondary" mb={4}>Sign in to your pharmacy workspace.</Typography><Box component="form" onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');try{const result=await api('/admin/login',{method:'POST',body:JSON.stringify({email,password})});sessionStorage.setItem('pharmacy.admin.token',result.token);onLogin(result.user);}catch(e){setError((e as Error).message);}finally{setBusy(false);}}}><Stack spacing={2.5}><TextField label="Email address" type="email" autoComplete="username" required value={email} onChange={e=>setEmail(e.target.value)}/><TextField label="Password" type="password" autoComplete="current-password" required value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<Alert severity="error">{error}</Alert>}<Button type="submit" variant="contained" size="large" disabled={busy}>{busy?'Signing in…':'Sign in to dashboard'}</Button><Typography variant="caption" color="text.secondary">Local setup: generated sign-in details are in apps/api/data/local-access.json. Ask your administrator for access in a shared environment.</Typography></Stack></Box></Box></Box>;
}
