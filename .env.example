# Copy to apps/mobile/.env. Public values only; never put secrets here.
EXPO_PUBLIC_DATA_MODE=mock
# To connect the shared API, change above to api and uncomment:
# EXPO_PUBLIC_API_URL=http://127.0.0.1:4000
# Use your computer LAN IP for a physical phone.
